# MongoDB Atlas connection string
# Replace <username>, <password>, and <cluster-url> with your actual values
DATABASE_URI=mongodb+srv://<username>:<password>@<cluster-url>/crypto-ape-blog?retryWrites=true&w=majority

# Generate a secure secret with: openssl rand -base64 32
PAYLOAD_SECRET=your-secret-key-here-change-this-in-production

# Server URL (use your production URL when deploying)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000